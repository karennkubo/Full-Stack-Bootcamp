```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if (quantidade < 12) {
    let maca = 1.30
    let compra = maca * quantidade
    return compra
  } else {
    let maca = 1
    let compra = maca * quantidade
    return compra
  }
  
}
```